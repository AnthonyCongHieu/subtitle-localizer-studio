# T00 synthetic fixtures

Run `python scripts/t00/generate_fixtures.py` to produce ignored CFR and VFR
MP4 fixtures, a UTF-8 SRT containing Chinese, Japanese, Korean, English, and
Vietnamese text, SHA-256 values, and ffprobe JSON in `generated/`.

The media bytes are generated locally and deliberately are not committed.
